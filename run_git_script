pipeline{
  agent any

  stages{
    stage(build ){
      steps {
        git 'https://github.com/madhbhat/repo.git'
        sh hello.sh
      }
    }

    stage(deploy){
      steps {
        echo " deploy"
      }
    }
  }
  
}
